# 踩坑记录

> 每次遇到问题，先搜索再看是否有人踩过；解决后登记，避免二次踩坑。

## 2026-08-11

- 日期：2026-08-11
- 问题描述：Java 编译报「非法字符: '\ufeff'」
- 报错 / 现象：maven-compiler 编译失败，`EnterpriseAgentApplication.java:[1,1] 非法字符: '\ufeff'`
- 排查过程：PowerShell `Set-Content -Encoding UTF8`（Windows PowerShell 5.1）会写入 UTF-8 BOM，Java 源码不接受
- 解决方案：用 `[System.IO.File]::WriteAllText(path, content, (New-Object System.Text.UTF8Encoding($false)))` 写无 BOM UTF-8
- 涉及技术：PowerShell / Java / Maven

- 日期：2026-08-11
- 问题描述：从 GitHub 下载 Temurin JDK 21 极慢
- 报错 / 现象：api.adoptium.net 直连 10 分钟只下载 4MB
- 排查过程：GitHub release 分发在国内网络不稳定
- 解决方案：换清华镜像 `https://mirrors.tuna.tsinghua.edu.cn/Adoptium/21/jdk/x64/windows/`，2 秒下载完成
- 涉及技术：网络 / 镜像源

- 日期：2026-08-11
- 问题描述：start.spring.io 无法生成 Spring Boot 3.5.x 项目
- 报错 / 现象：`Invalid Spring Boot version '3.5.16', Spring Boot compatibility range is >=4.0.0`（HTTP 400）
- 排查过程：2026 年起 start.spring.io 只提供 4.x 版本
- 解决方案：手写 pom.xml + 主类（parent 指定 3.5.16），等价于 initializr 产物
- 涉及技术：Spring Boot / Maven

## 2026-08-12

- 日期：2026-08-12
- 问题描述：LangChain4j 1.18 编译报「找不到符号 generate(List<ChatMessage>)」
- 报错 / 现象：`ChatService.java:[32,49] 找不到符号 方法 generate(java.util.List<ChatMessage>)`
- 排查过程：LangChain4j 1.x 重构了模型 API，旧 0.x 的 `ChatLanguageModel.generate()` 已移除，改用 `ChatModel.chat()`；用 `javap` 查看了 1.18.1 的 `ChatModel` / `ChatResponse` / `OpenAiChatModelBuilder` 方法签名确认
- 解决方案：`ChatResponse response = chatModel.chat(chatMessages); String reply = response.aiMessage().text();`；测试用 `ChatResponse.builder().aiMessage(new AiMessage("...")).build()` 构造 mock 返回值
- 涉及技术：LangChain4j 1.x / Java

- 日期：2026-08-12
- 问题描述：PowerShell 5.1 Invoke-RestMethod 显示中文乱码
- 报错 / 现象：调用 /chat 返回的中文回复显示为乱码（如 `ææ¯ä½ çä¼ä¸æºè½å©æ`）
- 排查过程：服务端返回的是标准 UTF-8 JSON，是 PowerShell 客户端解码方式的问题（把 UTF-8 当 ANSI 显示）
- 解决方案：用 curl.exe 验证线上数据正确；日常测试可用 curl 或新版 PowerShell
- 涉及技术：PowerShell / HTTP 编码

## 已记录
以上 6 条。
