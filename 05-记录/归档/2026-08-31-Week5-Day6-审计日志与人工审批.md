# Week 5 Day 6 归档：审计日志 + 高危操作人工审批流（2026-08-31）

> 学习计划：`01-每周学习/Week-05-企业级Agent安全/学习目标.md` ｜ 笔记：`02-知识库/Agent安全/Day6-审计日志与人工审批.md`

## 今天做了什么

1. 实现 `AuditLogService` 与 `AuditLogEntry`：记录谁、在哪个租户、调了什么工具、结果与状态。
2. 实现 `HumanApprovalService`：高危操作先生成审批单，人工 approve 后才放行。
3. 实现 `ApprovalAwareOrderTools`：`updateOrderStatus` 先审批后执行，并全程写审计。
4. 接入 `ApprovalAwareOrderAgentService`，用真实 DeepSeek 验证完整流程。

## 验证

- `HumanApprovalServiceTest`：审批前未授权、审批后已授权。
- `AuditLogServiceTest`：正确记录主体、租户、工具与状态。
- `ApprovalAwareOrderToolsTest`：第一次调用生成审批且数据不变，审批后再次调用才执行。
- `ApprovalFlowLiveTest`（真实 DeepSeek）：审批前 PENDING、审批后 SUCCESS，审计包含两个阶段。

## 完成标准（Day 6）

- [x] 关键操作有审计日志
- [x] 高危操作需要人工审批后才执行
- [x] 有真实调用大模型的例子（`ApprovalFlowLiveTest`）

## 下一步（Day 7）

- Week 5 周总结 + 知识库更新
