# Week 6.5 Day 4 归档：Agent 轨迹、稳定性与成本联合评估（2026-09-14）

## 今日完成

1. 将评估拆为最终答案、外部业务结果、执行轨迹三层，整体结果使用 AND 硬门禁。
2. 新增 `evaluation.trajectory`：`AgentTrajectory`、`TrajectoryStep`、`TrajectoryRecorder`、`TrajectoryEvaluator`。
3. 轨迹记录 Agent、模型响应、Tool、参数摘要、状态、Token、成本、整体耗时和 Handoff。
4. 建立开发集 O1001 与独立留出集 O1002，并增加审批顺序留出用例。
5. 新增 `StabilityAnalyzer`，统计成功率、成功样本方差、平均成本、标准差、P95 延迟、平均 Token 和步骤数。
6. 新增 `AgentComparisonService`，使用 Pareto 支配而非任意加权总分比较 Single/Multi。
7. 将三个轨迹离线测试加入本地 `eval-ci.ps1` 和 GitHub Actions。

## 真实 DeepSeek 对比

相同 O1002 留出题、模型、参数、数据和验收标准，Single/Multi 交错各运行 5 次：

| 指标 | Single | Multi |
| --- | ---: | ---: |
| 成功率 | 5/5（100%） | 5/5（100%） |
| 平均 Token | 1383.0 | 1557.4 |
| 平均估算成本 USD | 0.00043068 | 0.00051794 |
| 平均延迟 | 1284.2ms | 2080.6ms |
| P95 延迟 | 1511ms | 2488ms |
| 平均步骤 | 3.0 | 5.0 |

结论：本次简单查询质量持平，Single-Agent 成本、延迟和步骤均更低，Pareto 支配 Multi-Agent。结论仅适用于本任务、模型配置和 5 次学习样本。

## 测试

```powershell
cd 04-项目\enterprise-agent
mvn test "-Dtest=TrajectoryEvaluatorTest,AgentComparisonReportTest,TrajectoryRecorderTest"
.\scripts\eval-ci.ps1
.\scripts\test-live.ps1 -Test AgentTrajectoryEvaluationLiveTest
mvn clean test
```

- Day 4 专项离线测试：12 个通过。
- CI 离线门禁：19 个通过。
- 真实 DeepSeek：1 个 JUnit LiveTest，通过；内部 Single/Multi 各运行 5 次，生成 10 条轨迹、记录 25 次模型响应。
- 全量 clean 回归：217 个测试执行，0 失败、0 错误，34 个外部条件 LiveTest 按设计跳过。

## 边界

- 5 次样本的 nearest-rank P95 等于最大值，只是学习期粗粒度指标。
- `taskCompleted` 目前由评估运行器提供，生产应由独立业务状态验证器确认。
- 价格沿用项目评估单价，必须保存版本与日期，不能视为永久价格。
- Tool 参数当前是截断 JSON，生产应按字段白名单脱敏。

## 下一步

Week 6.5 Day 5：MCP 新规范与第三方 MCP 安全接入。
