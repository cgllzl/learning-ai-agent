# Week 3 Day 1 归档：Embedding 与向量检索原理 + 选型（2026-08-18）

> 学习计划：`01-每周学习/Week-03-RAG/学习目标.md` ｜ 笔记：`02-知识库/RAG/Day1-Embedding与向量检索原理.md`

## 今天学了什么

1. 为什么需要 RAG：模型不知道内部知识 → 幻觉/拒绝/过期；对比微调、长上下文硬塞。
2. Embedding：文本 → 向量，语义相近距离近；三种相似度（余弦/点积/欧氏）。
3. 向量检索：ANN 近似最近邻（HNSW 等），取 TopK。
4. 选型决策：
   - 学习期：InMemoryEmbeddingStore + all-MiniLM-L6-v2（本地 ONNX，零依赖零成本，DeepSeek 无 Embedding API）
   - 生产期：Qdrant / pgvector（LangChain4j EmbeddingStore 抽象，切换只改配置）
5. 踩坑：`langchain4j-embeddings-all-minilm-l6-v2` 只有 1.18.1-beta28 预发布版。

## 完成标准（Day 1）

- [x] 理解 Embedding / 向量 / 相似度
- [x] 完成向量库选型
- [x] 通读 LangChain4j RAG 官方文档要点

## 下一步（Day 2）

- 跑通文档入库：分块 → Embedding → 写入向量库（引入 all-minilm 依赖 + InMemoryEmbeddingStore）