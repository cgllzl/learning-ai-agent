# Week 2 Day 5 归档：修改订单状态（2026-08-17）

> 学习计划：`01-每周学习/Week-02-Tool-Calling/学习目标.md` ｜ 笔记：`02-知识库/Tool-Calling/Day5-修改订单状态与权限.md`

## 今天做了什么

1. `MockOrderData` 增加 `updateOrderStatus(orderId, newStatus)`。
2. `OrderTools` 新增 `updateOrderStatus` 工具，三重防线：状态枚举校验 → 订单存在性 → 仅 PENDING 可改。
3. 测试：`OrderToolsTest` 扩展到 9 个（修改状态 4 种规则）；`OrderAgentLiveTest` 扩展到 4 个（含改状态真实联调）。

## 验证结果

- `mvn test` 全部通过
- 真实 DeepSeek 联调：「把订单 O1003 的状态改为 SHIPPED」→ 模型调用 `updateOrderStatus`，回复「已成功修改为 SHIPPED」

## 完成标准（Day 5）

- [x] 修改订单状态（带参数校验与权限）
- [x] 越权修改（非 PENDING）被拒绝

## 下一步（Day 6）

- 处理 Tool 报错、超时、Tool 选择错误的兜底 + 最大迭代次数限制