# 学习项目协作约定

## 内容要求

- 每天学习的新知识，除了概念讲解和代码示例，都必须配一个「真实调用大模型」的例子。
- 调用大模型的例子要有可直接运行的测试（如 `xxxLiveTest`）或联调命令，并在当天知识库笔记里写清测试方式。
- 无法访问真实大模型时，要明确说明原因，并用可运行的本地替代验证，不能只停留在概念层。
- 每天的示例不能只有「便于理解的学习例子」，还要有一个贴合企业真实场景的「企业例子」，
  把技术点落到生产化问题里（例如权限审批、租户数据隔离、审计合规、成本核算等）。
