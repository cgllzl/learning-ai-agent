# 本机环境（2026-08-11 记录）

> 归档：`05-记录/归档/2026-08-11-Day1-环境搭建.md`

## JDK
- 安装：Temurin 21.0.12+8 LTS
- 位置：`G:\Environment\Java\temurin-21`
- JAVA_HOME：`G:\Environment\Java\temurin-21`（用户级环境变量已设置）
- 注意：机器级 PATH 里还有 `G:\Environment\Java\jdk-1.8\bin`，新开终端 `java -version` 可能仍是 JDK 8；项目用 `scripts\run-dev.ps1` 启动可强制使用 JAVA_HOME。要全局默认 21，需管理员在系统环境变量移除 jdk-1.8 的 PATH 项。

## Maven
- 版本：3.8.4（`G:\Environment\Maven\apache-maven-3.8.4`）
- 本地仓库：`G:\Environment\Maven\apache-maven-3.8.4\maven-repo`
- Java：已确认运行在 21.0.12

## Docker
- 状态：未安装（2026-08-11）
- 安装脚本：`04-项目/enterprise-agent/scripts/install-docker.ps1`（需管理员）
- 步骤：启用 WSL2 功能 → 装 WSL2 内核 → 静默安装 Docker Desktop → 重启 → 验证 `docker --version`
- 备用源：若安装包下载慢，可改用国内镜像手动下载 Docker Desktop 安装包

## 网络与镜像
- Maven Central 直连可用（repo1.maven.org 速度尚可）
- JDK 下载建议清华镜像：`https://mirrors.tuna.tsinghua.edu.cn/Adoptium/`
